rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // This phase has no participant uploads. Storage stays closed by default.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
